# API base URL used by server-side route handlers and guard checks.
ADMIN_API_BASE_URL=http://localhost:30011

# Optional fallback for legacy client requests.
NEXT_PUBLIC_API_BASE_URL=http://localhost:30011

# Comma-separated super-admin email allowlist for apps/admin.
ADMIN_SUPER_EMAIL_ALLOWLIST=you@eggturtle.local

# Backward-compatible alias (optional):
# ADMIN_SUPER_ADMIN_EMAILS=you@eggturtle.local

# UI hint flag only. Real access control is server-side.
NEXT_PUBLIC_SUPER_ADMIN_ENABLED=false
