# Backend (legacy FastAPI) example env (DO NOT commit real secrets)
# Copy to backend/.env for local dev.

# Database connection (default legacy uses sqlite)
DATABASE_URL=sqlite:///./data/app.db

# Security (set strong values in real deployments)
SECRET_KEY=__CHANGE_ME__

# Admin login (do NOT use weak defaults outside local dev)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=__CHANGE_ME__

# Upload directory
UPLOAD_DIR=./data/images
