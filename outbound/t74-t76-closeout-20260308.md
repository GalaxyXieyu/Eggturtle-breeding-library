# T74/T75/T76 closeout assessment (2026-03-08)

## Scope
- Repo: `/Users/apple/coding/Eggturtle-breeding-library`
- Assessed against current workspace state without reverting unrelated user changes.
- Focus files: `apps/web/app/app/[tenantSlug]/breeders/[id]/page.tsx`, `apps/web/components/breeder-detail/BreederInfoCard.tsx`, `apps/web/components/breeder-detail/TreeCard.tsx`, `docs/plan/EggsTask.csv`

## Git state observed
- Relevant tracked local modifications exist in the breeder detail page, info card, tree card, and task CSV.
- No rollback performed.

## Task assessment

### T74 - Breeder detail duplicate info cleanup
Status: done

Why:
- `apps/web/components/breeder-detail/BreederInfoCard.tsx` removes the duplicated summary cards for status / days since egg / last egg-mating that had already been introduced into the detail flow by T71.
- The card now keeps only the minimal top-of-page identity block: status badges, sex, series, name, relationship pills, description, and action buttons.
- This matches the stated goal of removing repeated first-screen information while preserving key business context.

### T75 - Compact relation pill styling
Status: done

Why:
- `apps/web/components/breeder-detail/BreederInfoCard.tsx` changes the relation pills from full-width grid pills to compact inline pills.
- Pill density is reduced via smaller vertical padding, smaller type size, lighter background, and flex-wrap layout.
- The pills remain readable and keep a stable touch/click target as non-interactive summary chips.

### T76 - Family tree production image rendering investigation
Status: doing

Why:
- `apps/web/components/breeder-detail/TreeCard.tsx` has been improved to reuse `FamilyNodeCard` and resolve image URLs through `resolveAuthenticatedAssetUrl`, which is a plausible fix for authenticated asset rendering.
- However, the task's done condition explicitly requires production or production-like verification to determine whether the issue is caused by data, signed URL generation, domain reachability, or render chain behavior.
- This run only verified code path and lint; no production environment, signed URL sample, or same-config browser proof was available in the repo.
- Therefore the root cause is not fully closed, even though the current code is directionally correct.

## Minimal verification run
- Command: `pnpm --filter @eggturtle/web lint`
- Result: pass with existing `@next/next/no-img-element` warnings only; no new lint failure from the breeder detail / family tree changes.

## Remaining gap for T76
- Need one same-config or production check that opens breeder family tree and confirms node images load with authenticated URLs.
- If it still fails, capture one real failing image URL pattern / network response to distinguish:
  - missing `coverImageUrl` data
  - bad signed URL generation
  - wrong public domain/base URL
  - auth/cors/caching issue
