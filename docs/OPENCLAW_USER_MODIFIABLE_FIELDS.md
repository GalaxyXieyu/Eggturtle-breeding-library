# OpenClaw 产品上传 - 用户可修改字段完整清单

## 概述

本文档详细列出了用户通过 OpenClaw skill 可以修改的所有产品字段，包括容易忽略的字段，以及如何通过接口查询生产环境数据。

## 一、用户可修改的所有字段

### 1. 基础信息（必填）

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `code` | String | 产品编号（唯一） | "CBF-001" | ❌ |
| `name` | String | 产品名称 | "陶瓷喂食碗" | ❌ |
| `shape` | String | 形状 | "圆形" | ❌ |
| `material` | String | 材质 | "陶瓷" | ❌ |
| `factory_price` | Float | 出厂价格 | 25.50 | ❌ |

### 2. 基础信息（可选但重要）

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `description` | Text | 产品描述 | "高品质陶瓷喂食碗..." | ⚠️ **是** - 影响 SEO |
| `series_id` | String | 所属系列 ID | "uuid-123" | ⚠️ **是** - 影响分类 |

### 3. 龟类专用字段（product_type = 'turtle'）

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `sex` | String | 性别 | "male" / "female" | ❌ |
| `offspring_unit_price` | Float | 后代单价（仅母龟） | 500.00 | ⚠️ **是** - 仅母龟需要 |
| `sire_code` | String | 父本编号 | "TURTLE-M-001" | ✅ **是** - 血统信息 |
| `dam_code` | String | 母本编号 | "TURTLE-F-001" | ✅ **是** - 血统信息 |
| `sire_image_url` | String | 父本图片 URL | "images/..." | ✅ **是** - 增加价值 |
| `dam_image_url` | String | 母本图片 URL | "images/..." | ✅ **是** - 增加价值 |

### 4. 产品分类字段

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `product_type` | String | 产品类型 | "tube" / "box" / "turtle" | ⚠️ **是** - 决定其他字段 |
| `tube_type` | String | 管型类型 | "圆管" / "方管" | ⚠️ **是** - 仅 tube 需要 |
| `box_type` | String | 盒型类型 | "翻盖盒" / "抽屉盒" | ⚠️ **是** - 仅 box 需要 |
| `process_type` | String | 工艺类型 | "注塑" / "吹塑" | ⚠️ **是** - 影响成本 |
| `functional_designs` | String | 功能设计（逗号分隔） | "透气孔,防滑底" | ✅ **是** - 卖点信息 |

### 5. 尺寸信息（dimensions JSON 对象）

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `dimensions.weight` | Float | 重量（kg） | 0.5 | ⚠️ **是** - 影响运费 |
| `dimensions.length` | Float | 长度（cm） | 15.0 | ⚠️ **是** - 影响运费 |
| `dimensions.width` | Float | 宽度（cm） | 15.0 | ⚠️ **是** - 影响运费 |
| `dimensions.height` | Float | 高度（cm） | 5.0 | ⚠️ **是** - 影响运费 |
| `dimensions.capacity.min` | Float | 最小容量（ml） | 100.0 | ✅ **是** - 产品规格 |
| `dimensions.capacity.max` | Float | 最大容量（ml） | 200.0 | ✅ **是** - 产品规格 |
| `dimensions.compartments` | Integer | 分隔数量 | 3 | ✅ **是** - 产品特性 |

### 6. 价格与成本

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `cost_price` | Float | 成本价 | 15.00 | ⚠️ **是** - 利润计算 |
| `factory_price` | Float | 出厂价（必填） | 25.50 | ❌ |

### 7. 包装与物流

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `has_sample` | Boolean | 是否有样品 | true / false | ⚠️ **是** - 客户询问 |
| `box_dimensions` | String | 纸箱尺寸 | "50x40x30cm" | ✅ **是** - 物流计算 |
| `box_quantity` | Integer | 装箱数量 | 50 | ✅ **是** - 批量订单 |

### 8. 库存与展示

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `in_stock` | Boolean | 是否有货 | true / false | ⚠️ **是** - 影响销售 |
| `popularity_score` | Integer | 热度评分（0-100） | 85 | ✅ **是** - 排序权重 |
| `is_featured` | Boolean | 是否精选 | true / false | ⚠️ **是** - 首页展示 |

### 9. 产品图片（product_images 表）

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `images[].url` | String | 图片 URL | "images/CBF-001/main.jpg" | ❌ |
| `images[].alt` | String | 图片描述 | "陶瓷喂食碗主图" | ⚠️ **是** - SEO 优化 |
| `images[].type` | String | 图片类型 | "main" / "gallery" / "dimensions" / "detail" | ⚠️ **是** - 展示位置 |
| `images[].sort_order` | Integer | 排序顺序 | 0, 1, 2... | ⚠️ **是** - 展示顺序 |

### 10. 系列信息（series 表 - 间接修改）

| 字段 | 类型 | 说明 | 示例 | 容易忽略？ |
|------|------|------|------|-----------|
| `series.code` | String | 系列编号 | "SER-TURTLES-001" | ⚠️ **是** - 稳定标识 |
| `series.name` | String | 系列名称 | "CB-2026" | ❌ |
| `series.description` | Text | 系列描述 | "2026年繁殖季..." | ✅ **是** - 系列介绍 |
| `series.sort_order` | Integer | 排序顺序 | 0 | ✅ **是** - 展示顺序 |
| `series.is_active` | Boolean | 是否激活 | true / false | ⚠️ **是** - 是否显示 |

## 二、容易忽略的字段（重点关注）

### 🔴 高优先级（严重影响业务）

1. **`description`** - 产品描述
   - **影响**: SEO 排名、用户理解、转化率
   - **建议**: 20-500 字，包含关键词和卖点
   - **示例**: "高品质陶瓷喂食碗，适用于各类爬行动物。耐用、易清洗、防滑底设计。"

2. **`in_stock`** - 库存状态
   - **影响**: 用户能否下单、库存管理
   - **建议**: 及时更新，避免缺货订单
   - **默认**: true

3. **`is_featured`** - 是否精选
   - **影响**: 首页展示、推荐位
   - **建议**: 精选优质产品，提高转化
   - **默认**: false

4. **`images[].type`** - 图片类型
   - **影响**: 图片展示位置和用途
   - **建议**: 至少 1 张 main，2-4 张 gallery
   - **类型**: main（主图）、gallery（相册）、dimensions（尺寸图）、detail（细节图）

5. **`images[].alt`** - 图片描述
   - **影响**: SEO、无障碍访问
   - **建议**: 描述性文字，包含产品名称
   - **示例**: "陶瓷喂食碗主图 - 圆形设计"

### 🟡 中优先级（影响用户体验）

6. **`series_id`** - 所属系列
   - **影响**: 产品分类、筛选、推荐
   - **建议**: 为每个产品分配系列
   - **查询**: `GET /api/series` 获取可用系列

7. **`cost_price`** - 成本价
   - **影响**: 利润计算、定价策略
   - **建议**: 准确填写，用于内部分析
   - **默认**: 0.0

8. **`dimensions`** - 尺寸信息
   - **影响**: 运费计算、产品规格展示
   - **建议**: 物理产品必填
   - **字段**: weight, length, width, height, capacity, compartments

9. **`has_sample`** - 是否有样品
   - **影响**: 客户询问、样品管理
   - **建议**: 及时更新样品状态
   - **默认**: false

10. **`box_dimensions` + `box_quantity`** - 包装信息
    - **影响**: 批量订单、物流计算
    - **建议**: 批发产品必填
    - **示例**: "50x40x30cm", 50 件/箱

### 🟢 低优先级（增值信息）

11. **`sire_code` + `dam_code`** - 血统信息（龟类）
    - **影响**: 产品价值、繁殖记录
    - **建议**: 有血统的龟类必填
    - **示例**: "TURTLE-M-001", "TURTLE-F-001"

12. **`sire_image_url` + `dam_image_url`** - 父母图片（龟类）
    - **影响**: 产品展示、信任度
    - **建议**: 提供父母照片增加价值
    - **示例**: "images/parents/sire-001.jpg"

13. **`offspring_unit_price`** - 后代单价（母龟）
    - **影响**: 繁殖业务、定价
    - **建议**: 仅母龟填写
    - **示例**: 500.00

14. **`functional_designs`** - 功能设计
    - **影响**: 产品卖点、筛选
    - **建议**: 列出主要功能特性
    - **示例**: "透气孔,防滑底,易清洗"

15. **`popularity_score`** - 热度评分
    - **影响**: 排序权重、推荐算法
    - **建议**: 根据销量和浏览量设置
    - **范围**: 0-100

16. **`process_type`** - 工艺类型
    - **影响**: 成本分析、产品分类
    - **建议**: 生产相关产品填写
    - **示例**: "注塑", "吹塑", "手工"

17. **`tube_type` / `box_type`** - 具体类型
    - **影响**: 产品筛选、分类
    - **建议**: 根据 product_type 填写对应字段
    - **示例**: tube_type="圆管", box_type="翻盖盒"

18. **`series.description`** - 系列描述
    - **影响**: 系列页面展示
    - **建议**: 介绍系列特点和背景
    - **示例**: "2026年繁殖季，优质血统"

19. **`series.sort_order`** - 系列排序
    - **影响**: 系列展示顺序
    - **建议**: 重要系列设置较小值
    - **默认**: 0

20. **`images[].sort_order`** - 图片排序
    - **影响**: 图片展示顺序
    - **建议**: 主图 0，其他按重要性排序
    - **默认**: 自动递增

## 三、如何查询生产环境数据

### 方法 1: 通过 API 接口查询（推荐）

#### 1. 认证登录
```bash
curl -X POST https://turtlealbum.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "your_password"
  }'

# 返回:
{
  "access_token": "eyJhbGc...",
  "token_type": "bearer"
}
```

#### 2. 查询所有产品
```bash
curl -X GET "https://turtlealbum.com/api/products?page=1&page_size=100" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 返回:
{
  "products": [
    {
      "id": "uuid-123",
      "code": "CBF-001",
      "name": "陶瓷喂食碗",
      "description": "...",
      "shape": "圆形",
      "material": "陶瓷",
      "factory_price": 25.50,
      "dimensions": {...},
      "images": [...],
      ...
    }
  ],
  "total": 32,
  "page": 1,
  "total_pages": 1
}
```

#### 3. 查询单个产品
```bash
curl -X GET "https://turtlealbum.com/api/products/{product_id}" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 返回完整产品信息
```

#### 4. 查询所有系列
```bash
curl -X GET "https://turtlealbum.com/api/series" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 返回:
{
  "data": [
    {
      "id": "uuid-456",
      "code": "SER-TURTLES-001",
      "name": "CB-2026",
      "description": "...",
      "is_active": true,
      "sort_order": 0
    }
  ]
}
```

#### 5. 查询产品筛选选项
```bash
curl -X GET "https://turtlealbum.com/api/products/filter-options" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 返回所有可用的筛选值:
{
  "tube_types": ["圆管", "方管", ...],
  "box_types": ["翻盖盒", "抽屉盒", ...],
  "process_types": ["注塑", "吹塑", ...],
  "functional_designs": ["透气孔", "防滑底", ...],
  "shapes": ["圆形", "方形", ...],
  "materials": ["陶瓷", "塑料", ...],
  "capacity_range": {"min": 100, "max": 2000},
  "price_range": {"min": 10, "max": 5000}
}
```

### 方法 2: 通过 Python 脚本查询

创建查询脚本 `scripts/query_production.py`:

```python
import requests
import json
from typing import Optional

class TurtleAlbumAPI:
    def __init__(self, base_url: str, username: str, password: str):
        self.base_url = base_url
        self.token = None
        self.login(username, password)

    def login(self, username: str, password: str):
        """登录并获取 token"""
        response = requests.post(
            f"{self.base_url}/api/auth/login",
            json={"username": username, "password": password}
        )
        response.raise_for_status()
        self.token = response.json()["access_token"]
        print(f"✅ 登录成功")

    def get_headers(self):
        """获取请求头"""
        return {
            "Authorization": f"Bearer {self.token}",
            "Content-Type": "application/json"
        }

    def get_all_products(self, page: int = 1, page_size: int = 100):
        """获取所有产品"""
        response = requests.get(
            f"{self.base_url}/api/products",
            params={"page": page, "page_size": page_size},
            headers=self.get_headers()
        )
        response.raise_for_status()
        return response.json()

    def get_product(self, product_id: str):
        """获取单个产品"""
        response = requests.get(
            f"{self.base_url}/api/products/{product_id}",
            headers=self.get_headers()
        )
        response.raise_for_status()
        return response.json()

    def get_all_series(self):
        """获取所有系列"""
        response = requests.get(
            f"{self.base_url}/api/series",
            headers=self.get_headers()
        )
        response.raise_for_status()
        return response.json()

    def get_filter_options(self):
        """获取筛选选项"""
        response = requests.get(
            f"{self.base_url}/api/products/filter-options",
            headers=self.get_headers()
        )
        response.raise_for_status()
        return response.json()

    def search_products(self, code: Optional[str] = None, name: Optional[str] = None):
        """搜索产品"""
        products = self.get_all_products()
        results = []

        for product in products["products"]:
            if code and code.lower() in product["code"].lower():
                results.append(product)
            elif name and name.lower() in product["name"].lower():
                results.append(product)

        return results

# 使用示例
if __name__ == "__main__":
    # 连接生产环境
    api = TurtleAlbumAPI(
        base_url="https://turtlealbum.com",
        username="admin",
        password="your_password"
    )

    # 查询所有产品
    products = api.get_all_products()
    print(f"\n📦 总产品数: {products['total']}")

    # 查询所有系列
    series = api.get_all_series()
    print(f"\n📚 总系列数: {len(series['data'])}")
    for s in series["data"]:
        print(f"  - {s['code']}: {s['name']}")

    # 查询筛选选项
    options = api.get_filter_options()
    print(f"\n🔍 可用筛选选项:")
    print(f"  - 形状: {', '.join(options['shapes'])}")
    print(f"  - 材质: {', '.join(options['materials'])}")

    # 搜索特定产品
    results = api.search_products(code="CBF")
    print(f"\n🔎 搜索 'CBF' 找到 {len(results)} 个产品")
    for product in results:
        print(f"  - {product['code']}: {product['name']}")
```

运行脚本:
```bash
cd /Volumes/DATABASE/code/turtle_album
python3 scripts/query_production.py
```

### 方法 3: 通过 OpenClaw Skill 查询

在 OpenClaw skill 中添加查询功能:

```
User: /openclaw query-product CBF-001
Bot:
📦 产品信息: CBF-001

基础信息:
- 名称: 陶瓷喂食碗
- 描述: 高品质陶瓷喂食碗...
- 形状: 圆形
- 材质: 陶瓷
- 出厂价: ¥25.50

尺寸信息:
- 重量: 0.5 kg
- 尺寸: 15x15x5 cm

库存状态:
- 有货: ✅
- 有样品: ✅
- 精选产品: ❌

图片:
- 主图: 1 张
- 相册: 3 张
- 总计: 4 张

系列: CB-2026

⚠️ 缺失字段:
- cost_price (成本价)
- box_dimensions (纸箱尺寸)
- box_quantity (装箱数量)

质量评分: 8/10 (良好)
```

## 四、OpenClaw Skill 覆盖场景

### ✅ 已覆盖场景

1. **新产品上传**
   - 必填字段收集
   - 可选字段收集
   - 图片上传
   - 数据验证

2. **产品信息修改**
   - 通过 `PUT /api/admin/products/{product_id}` 更新
   - 支持部分字段更新

3. **图片管理**
   - 上传新图片
   - 删除图片
   - 设置主图
   - 重新排序

4. **环境切换**
   - dev / staging / prod
   - 生产环境确认

5. **数据验证**
   - 3 级验证规则
   - 质量评分
   - 缺失字段提醒

### ⚠️ 需要补充的场景

1. **批量上传**
   - 通过 Excel + ZIP 批量导入
   - 复用现有 `/api/products/batch-import` 接口

2. **产品查询**
   - 查询现有产品信息
   - 搜索产品
   - 查看缺失字段

3. **产品更新**
   - 更新现有产品字段
   - 补充缺失信息
   - 更新库存状态

4. **系列管理**
   - 创建新系列
   - 更新系列信息
   - 分配产品到系列

5. **数据导出**
   - 导出产品列表
   - 导出为 Excel
   - 数据备份

### 🔄 建议增强的功能

1. **智能提醒**
   - 定期检查缺失字段
   - 提醒更新库存状态
   - 提醒补充图片

2. **数据质量报告**
   - 生成质量评分报告
   - 列出需要改进的产品
   - 优先级排序

3. **模板支持**
   - 保存产品模板
   - 快速填充常用字段
   - 批量应用模板

4. **历史记录**
   - 查看修改历史
   - 回滚功能
   - 审计日志

## 五、实施建议

### 短期（1-2 周）

1. ✅ 完成基础上传功能（已完成）
2. 🔲 添加产品查询功能
3. 🔲 添加产品更新功能
4. 🔲 测试 dev 环境

### 中期（2-4 周）

5. 🔲 添加批量上传功能
6. 🔲 添加系列管理功能
7. 🔲 添加数据质量报告
8. 🔲 测试 staging 环境

### 长期（1-2 月）

9. 🔲 添加智能提醒功能
10. 🔲 添加模板支持
11. 🔲 添加历史记录和回滚
12. 🔲 生产环境上线

## 六、总结

### 关键要点

1. **必填字段**: code, name, shape, material, factory_price
2. **容易忽略**: description, images.alt, series_id, dimensions, in_stock, is_featured
3. **查询方式**: API 接口、Python 脚本、OpenClaw skill
4. **覆盖场景**: 新建、修改、查询、批量、系列管理

### 下一步行动

1. 在 OpenClaw skill 中添加产品查询功能
2. 在 OpenClaw skill 中添加产品更新功能
3. 创建 Python 查询脚本用于生产环境数据分析
4. 定期生成数据质量报告，提醒补充缺失字段
